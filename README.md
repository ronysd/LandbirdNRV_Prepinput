After knitting and rendering `bird_dataPrep.Rmd`, overwrite this file with a symlink to `bird_dataPrep.md`.

E.g., on Linux/macOS from terminal:

```bash
cd /home/sdas/bird_NRV/bird_dataPrep
rm README.md && ln -s bird_dataPrep.md README.md
```
